<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.neotreks.accuterra.mobile.demo.profile.ProfileActivity">

    <!-- TOOLBAR -->

     <LinearLayout
        android:id="@+id/activity_profile_toolbar_envelope"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:orientation="vertical">

        <include
            layout="@layout/accuterra_toolbar"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <include layout="@layout/component_basic_tabs" />

    </LinearLayout>

    <!-- SETTINGS -->

    <TableLayout
        android:id="@+id/activity_profile_settings_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/activity_profile_toolbar_envelope"
        android:layout_margin="@dimen/default_margin"
        android:stretchColumns="1">

        <!-- SETTINGS -->
        <TableRow>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/general_settings" />

            <ImageButton
                android:id="@+id/activity_profile_settings_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_settings_black_24dp"
                android:layout_gravity="end"/>

        </TableRow>

        <!-- RESET TOKEN -->
        <TableRow>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/upload_reset_access_token" />

            <ImageButton
                android:id="@+id/activity_profile_reset_token_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:src="@drawable/ic_offline_bolt_black_24dp" />

        </TableRow>

    </TableLayout>

</androidx.constraintlayout.widget.ConstraintLayout>