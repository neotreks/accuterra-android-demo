ext {
    // Readonly access to Maven repo via our `distribution site`
    dist_username = '***'
    dist_password = '***'
    // URL to AccuTerra BE
    ws_base_url = '"***"'
    // URL and credentials for the authentication service
    ws_auth_url = '"***"'
    ws_auth_client_id = '"***"'
    ws_auth_client_secret = '"***"'
    // MapBox
    mapbox_token = '"***"'
    // HERE Maps
    heremaps_appid = '"***"'
    heremaps_appcode = '"***"'
}